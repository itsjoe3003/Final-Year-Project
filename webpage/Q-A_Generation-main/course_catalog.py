# course_catalog.py

import streamlit as st

def course_catalog():
    st.title("Course Catalog")
    st.write("List of available courses.")

    # Add course cards or table with course details (title, description, instructor, etc.)

if __name__ == "__main__":
    course_catalog()
